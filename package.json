{
    "name": "opticam-root",
    "version": "1.0.0",
    "description": "Root script to run full stack OptiCam app",
    "scripts": {
        "install:all": "cd backend && npm install && cd ../frontend && npm install",
        "dev": "concurrently \"cd backend && node server.js\" \"cd frontend && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}